<%= form_for(@lock_request) do |f| %>
  <% if @lock_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lock_request.errors.count, "error") %> prohibited this lock_request from being saved:</h2>

      <ul>
      <% @lock_request.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user %><br>
    <%= f.text_field :user %>
  </div>
  <div class="field">
    <%= f.label :isLocked %><br>
    <%= f.check_box :isLocked %>
  </div>
  <div class="field">
    <%= f.label :willLock %><br>
    <%= f.check_box :willLock %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
